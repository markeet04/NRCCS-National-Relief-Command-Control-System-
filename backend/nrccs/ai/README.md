# This file should contain the trained model (model.pkl)
# Copy the model.pkl file from Artiint/ folder here before building Docker image
# 
# To copy the model:
# cp ../../Artiint/model.pkl ./model.pkl
#
# Or run the copy script:
# ./copy-ai-assets.ps1
